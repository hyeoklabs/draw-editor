# Draw Editor 수동 테스트 시나리오

이 문서는 리팩토링 이후 기능 회귀(regression)를 확인하기 위한 수동 테스트 체크리스트입니다.

## 1) 기본 로딩
1. 페이지 진입 시 이미지가 정상 로드된다.
2. 캔버스가 `canvas-wrap` 안에 표시된다.
3. 초기 상태에서 Save 버튼은 비활성화된다.

## 2) 체크/지우기
1. `Check` 모드에서 탭/클릭 시 마크가 추가된다.
2. `Single` 모드에서 새 체크 시 기존 체크가 제거된다.
3. `Multi` 모드에서 여러 체크가 누적된다.
4. `Erase` 모드에서 체크 근처를 누르면 체크가 제거된다.

## 3) Save 버튼 상태 이벤트
1. 체크가 1개 이상이면 Save 버튼이 활성화된다.
2. `Reset`으로 체크를 모두 지우면 Save 버튼이 비활성화된다.
3. 저장 중에는 Save 버튼이 `Saving...`으로 표시되고 비활성화된다.

## 4) 줌/팬
1. 핀치로 확대/축소가 가능하다.
2. 확대 상태에서 1손가락 팬이 동작한다.
3. 줌/팬 후에도 캔버스가 표시 영역 밖으로 벗어나지 않는다.
4. 안드로이드 태블릿에서 확대 상태 탭 체크가 간헐적으로 누락되지 않는다.

## 5) 저장/복원
1. Save 클릭 시 에러 없이 저장된다.
2. 저장 후 미리보기 이미지가 갱신된다.
3. 저장된 이미지 클릭 시 캔버스에 다시 로드된다.

## 6) 예외/오류 처리
1. IndexedDB 실패 상황에서 에러 문구가 표시된다.
2. 저장 실패 후에도 앱이 멈추지 않고 재시도가 가능하다.

## 7) 메모리/리소스
1. 화면 이동(언마운트) 시 오류가 발생하지 않는다.
2. 재진입 후 캔버스가 중복 생성되지 않는다.

